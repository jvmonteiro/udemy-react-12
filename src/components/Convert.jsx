import React, { useState, useEffect } from "react";

/* Can't continue due to Trasnlate API being paid */
const Convert = ({ language, text }) => {
  useEffect(() => {}, [language, text]);

  return (
    <div>
      <div> Convert</div>
    </div>
  );
};

export default Convert;
